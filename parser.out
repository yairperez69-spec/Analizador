tokens = (
    'INT', 'IF', 'IDENTIFIER', 'NUMBER',
    'PLUS', 'MINUS', 'MULTIPLY', 'DIVIDE',
    'ASSIGN', 'SEMICOLON',
    'LPAREN', 'RPAREN', 'LBRACE', 'RBRACE',
    'GREATERTHAN'
)

t_PLUS       = r'\+'
t_MINUS      = r'-'
t_MULTIPLY   = r'\*'
t_DIVIDE     = r'/'
t_ASSIGN     = r'='
t_SEMICOLON  = r';'
t_LPAREN     = r'\('
t_RPAREN     = r'\)'
t_LBRACE     = r'\{'
t_RBRACE     = r'\}'
t_GREATERTHAN = r'>'

def t_INT(t):
    r'int'
    return t

def t_IF(t):
    r'if'
    return t

def t_IDENTIFIER(t):
    r'[a-zA-Z_][a-zA-Z0-9_]*'
    return t

def t_NUMBER(t):
    r'\d+'
    return t

t_ignore = ' \t\n'

def t_error(t):
    print(f"Carácter ilegal: {t.value[0]}")
    t.lexer.skip(1)
